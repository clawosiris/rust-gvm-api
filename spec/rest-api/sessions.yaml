# SPDX-License-Identifier: AGPL-3.0-or-later
# SPDX-FileCopyrightText: 2026 Greenbone AG

openapi: 3.1.0

# Public REST session endpoints are intentionally deferred from the current
# delivered Targets CRUD scope. The gateway still uses an internal
# session-backed execution model, but token provisioning and lifecycle are not
# yet exposed as REST resources.

paths: {}

components:
  schemas: {}
