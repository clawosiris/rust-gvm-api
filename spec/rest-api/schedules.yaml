# SPDX-License-Identifier: AGPL-3.0-or-later
# SPDX-FileCopyrightText: 2026 Greenbone AG

openapi: 3.1.0

paths:
  /timezones:
    get:
      operationId: getTimezones
      tags: [Schedules]
      summary: List available schedule timezones
      description: Returns the timezone identifiers accepted by the backend for schedule resources.
      responses:
        "200":
          description: Supported schedule timezones
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TimezoneList"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"

  /schedules:
    get:
      operationId: getSchedules
      tags: [Schedules]
      summary: List schedules
      parameters:
        - $ref: "./common.yaml#/components/parameters/FilterString"
        - $ref: "./common.yaml#/components/parameters/FilterId"
        - $ref: "./common.yaml#/components/parameters/Page"
        - $ref: "./common.yaml#/components/parameters/PerPage"
      responses:
        "200":
          description: Paginated list of schedules
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScheduleList"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
    post:
      operationId: createSchedule
      tags: [Schedules]
      summary: Create a schedule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSchedule"
      responses:
        "201":
          description: Schedule created
          headers:
            Location:
              $ref: "./common.yaml#/components/headers/Location"
          content:
            application/json:
              schema:
                $ref: "./common.yaml#/components/schemas/ResourceCreated"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"

  /schedules/{id}:
    parameters:
      - $ref: "./common.yaml#/components/parameters/ResourceId"
    get:
      operationId: getSchedule
      tags: [Schedules]
      summary: Get a schedule
      responses:
        "200":
          description: Schedule details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Schedule"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
    put:
      operationId: modifySchedule
      tags: [Schedules]
      summary: Modify a schedule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ModifySchedule"
      responses:
        "200":
          description: Schedule updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Schedule"
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"
    delete:
      operationId: deleteSchedule
      tags: [Schedules]
      summary: Delete a schedule
      responses:
        "204":
          description: Schedule deleted
        "400":
          $ref: "./common.yaml#/components/responses/BadRequest"
        "401":
          $ref: "./common.yaml#/components/responses/Unauthorized"
        "404":
          $ref: "./common.yaml#/components/responses/NotFound"

components:
  schemas:
    Schedule:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        comment:
          type: string
        icalendar:
          type: string
          description: iCalendar string defining the schedule recurrence.
        timezone:
          type: string
          description: IANA time zone identifier.
          examples:
            - "Europe/Berlin"
        firstRun:
          type: string
          format: date-time
        nextRun:
          type: string
          format: date-time
        duration:
          type: integer
          description: Maximum run duration in seconds. 0 means unlimited.
        inUse:
          type: boolean
        writable:
          type: boolean

    CreateSchedule:
      type: object
      required:
        - name
        - icalendar
        - timezone
      properties:
        name:
          type: string
        comment:
          type: string
        icalendar:
          type: string
          description: iCalendar string defining the schedule recurrence.
        timezone:
          type: string
          description: IANA time zone identifier.

    ModifySchedule:
      type: object
      properties:
        name:
          type: string
        comment:
          type: string
        icalendar:
          type: string
        timezone:
          type: string

    ScheduleList:
      type: object
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Schedule"
        pagination:
          $ref: "./common.yaml#/components/schemas/Pagination"

    Timezone:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          description: IANA timezone identifier accepted by the backend.
          examples:
            - "Europe/Berlin"
        displayName:
          type: string
          description: Optional human-friendly label for UI presentation.

    TimezoneList:
      type: object
      required:
        - data
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Timezone"
