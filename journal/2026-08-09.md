# 2026-08-09 — Current rust-gvm Integration Reached a Green End-to-End State

## Dependency Integration
- PR [#437](https://github.com/greenbone-hive/rust-gvm-api/pull/437) opened against `main` to pin the five rust-gvm components atomically to the current `devel` revision and add a lockfile architecture guard that prevents their source SHAs from drifting apart.
- The gateway adapter now uses the current typed schedule, alert, credential, NVT, note, override, and target contracts while preserving omit, clear, and replacement semantics without moving GMP construction or parsing into this repository.

## Report and Runtime Contract
- Aggregate vulnerability results now preserve `hostsCount` and `occurrences`, omit absent singular host and port values, and expose nested NVT identity. Closed-CVE rows retain the CVE as the public result name while reporting NVT name and OID separately.
- Compose now pins an immutable gvmd 26.36.1 all-features image advertising GMP 22.8, satisfying rust-gvm's typed report-export compatibility boundary without an API-side version bypass.

## Validation State
- PR #437 is open, mergeable, and awaiting review. CI, packaging, security, static analysis, and the Compose-backed discovery scan, report retrieval, and report export lifecycle all pass.
