# 2026-05-27 — `main` Finally Absorbed the Security-Hardening Wave, and the Next REST Slice Immediately Escaped Back Into Branches

## Mainline & Merged Work

### The default branch moved materially on 2026-05-26, but the repo still used feature branches to carry the next round of work almost immediately
- PR #133 (`feat(rest): harden API security middleware`) merged into `main` on 2026-05-26 and finally delivered the deny-by-default CORS, audit-safety, rate-limiting, and protected-route hardening work that had been sitting in the issue backlog since 2026-05-22
- PR #144 (`feat(rest): implement RMM Level 2 REST contract`) landed less than an hour later, so `main` did not just take security plumbing; it also shipped another meaningful REST surface expansion in the same window
- PR #141 only consolidated prior journal entries, so the real substance here is that `main` absorbed two overdue product-facing slices on the same day and then stopped again

## Issues
- The merge of PR #133 closed issue #8 plus issues #128, #129, #131, and #132, which means the repo finally converted most of the Phase 3 security backlog from planning into shipped behavior
- Issue #142 (`document and unify REST problem-type taxonomy`) was opened and closed the same day, so that cleanup work was identified and resolved without lingering in the queue
- The backlog immediately re-expanded with issue #145 (Phase 5 telemetry refinement) and issue #147 (gvmd capability discovery and hypermedia differences), which keeps the roadmap moving forward even though the newly closed security tranche was substantial

## Pull Request Queue
- The next live implementation lane is now PR #146 (`feat(rest): implement phase 4 resource endpoints`), opened on 2026-05-26 after the `main` merges finished
- PR #148 (`docs(rest): add remaining issue 125 spec endpoints`) merged on 2026-05-27, but it merged into the Phase 4 branch rather than into `main`, so the branch queue is already accumulating follow-on work ahead of the next default-branch update
- Older open branches are still stacked underneath that path: PR #143 (cargo dependency refresh), PR #140 (MCP API-layer planning), PR #139 and #137 (maintenance updates), and PR #124 (release packaging)
- So the repo is healthier than yesterday, but not simpler: `main` caught up on a real delivery wave while the queue immediately re-formed around the next REST milestone

## CI Health
- The `main` push runs triggered by PRs #133, #141, and #144 all completed successfully on 2026-05-26 in both `CI` and `Security`, so the branch absorbed the new work cleanly
- `Dependabot Updates` on `main` also succeeded on 2026-05-26, which is a better maintenance signal than the mixed automation noise noted in the previous entry
- The newest completed `CI` and `Security` runs for PR #146 also passed, so the next implementation branch is entering review from a stable position rather than from a red build
