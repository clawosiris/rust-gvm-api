# 2026-08-07 — Endpoint PRs Retain Contract and Acceptance Blockers

## Host Write Contract
- PR [#426](https://github.com/greenbone-hive/rust-gvm-api/pull/426) recovered from its timing-sensitive stable-test failure after a rerun, returning the full reported CI matrix to green.
- A new review found that the narrowed host surface still silently accepts unsupported operations: unknown `value` input is discarded during host modification, while `ultimate=true` is ignored during deletion. The PR remains blocked until the REST and lower-layer boundaries reject these semantics and add regression coverage.

## Compliance Workflow
- PR [#418](https://github.com/greenbone-hive/rust-gvm-api/pull/418) fixed its earlier command-family problem by routing audit and policy item operations through usage-type-scoped paths.
- Review identified two remaining acceptance blockers: the scan-config collection can still leak policies because the pinned rust-gvm dependency does not scope the list to `usage_type="scan"`, and the E2E test does not execute the requested create-to-compliance-report workflow. The PR remains open with changes requested.
