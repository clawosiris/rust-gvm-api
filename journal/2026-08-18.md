# 2026-08-18 — Mainline Dependencies Advanced and API Surface Reached Devel

## Mainline Updates
- Pull request [#447](https://github.com/greenbone-hive/rust-gvm-api/pull/447) merged patch updates for `async-trait`, `base64`, `clap`, and `rcgen` into `main`.
- Pull request [#441](https://github.com/greenbone-hive/rust-gvm-api/pull/441) merged refreshed immutable pins for `Swatinem/rust-cache` and `github/codeql-action/upload-sarif` across CI, E2E, release, security, and performance workflows.
- CI, security, packaging, and REST discovery E2E validation passed for both mainline merges; the subsequent scheduled E2E run also remained green.

## Devel API Movement
- Pull request [#418](https://github.com/greenbone-hive/rust-gvm-api/pull/418) merged the audits and policies `usage_type` API surface into `devel` with its functional, security, packaging, and devel REST discovery E2E checks passing.
