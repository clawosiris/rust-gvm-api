# 2026-06-04 — `main` Shipped a Fast REST Surface Burst and Tightened Auth, Session, and Pagination Semantics

## Mainline & Merged Work

### The repo moved hard on 2026-06-03, and nearly all of that movement was user-facing REST work
- Since the 2026-06-02 entry, `main` absorbed six substantive merges in one day: PR #207 added supporting resource catalogs, PR #209 expanded E2E coverage for shipped identity/admin endpoints, PR #208 added note and override resources, PR #218 unified protected-route auth policy, PR #221 aligned pagination naming examples, and PR #220 switched the current-session lifecycle to singular session routes
- That set is coherent rather than random churn: the repo expanded the REST resource surface, then immediately tightened the contract details around authentication, pagination vocabulary, and session route naming
- In other words, the repo used one short window to make the public API broader and more internally consistent at the same time, instead of leaving follow-up cleanup to linger off-branch

## Issues
- Eight issues opened in this window: #206 (identity/admin E2E coverage), #210 (push pagination into gvmd-backed filters), #211 (protected-route auth classification), #212 (report subresource classification), #213 (stale spec-named crates and gRPC mismatch), #215 (singular current-session routes), #216 (query-parameter decoding consistency), and #217 (pagination field naming alignment)
- Five of those issues already closed through merged work on `main`: #206, #211, #213, #215, and #217
- Older REST-surface backlog also continued to burn down: issue #168 (report-format/filter/tag/ticket endpoints), #170 (note/override endpoints), and #172 (system-report/integration-config/aggregate/trashcan endpoints) all closed on 2026-06-03 as their corresponding work landed

## Pull Request Queue
- The open queue is now short and sharply defined: PR #214 carries host and NVT catalog reads, while PR #219 pushes REST pagination into backend filters instead of local re-pagination
- That is a healthier queue shape than the repo had at the start of June because the unresolved work is now two explicit follow-through branches, not a wide spread of contract and runtime ambiguity

## CI Health
- Verification stayed exceptionally clean during the burst: every substantive push in this window cleared `CI`, `E2E`, and `Security`
- Scheduled `E2E` on `main` also succeeded on 2026-06-03, so the integration baseline remained green while six merges landed in rapid sequence
- There is no comparable fresh CI noise in this window; the repo's current signal is strong enough that the remaining risk is review throughput on the two open PRs, not branch instability
