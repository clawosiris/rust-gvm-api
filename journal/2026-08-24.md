# 2026-08-24 — New h2 Advisory Breaks Mainline Security

## Security Regression
- Scheduled Security run [#32696168309](https://github.com/greenbone-hive/rust-gvm-api/actions/runs/32696168309) failed on unchanged `main` commit `c63babf0` after Cargo Audit detected `RUSTSEC-2026-0258` in `h2` 0.4.15.
- The advisory covers unbounded empty HTTP/2 DATA frames and is fixed in `h2` 0.4.16 or later.
- Unused-dependency and Semgrep checks remain green; Cargo Audit is the actionable blocker that causes the aggregate Security job to fail.

## Stable Validation
- Daily REST discovery E2E runs and the weekly performance run continued to pass on the same mainline commit, isolating the regression to the newly disclosed dependency vulnerability.
