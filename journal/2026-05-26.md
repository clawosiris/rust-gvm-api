# 2026-05-26 — `main` Landed Request-Scoped Auth, Then the Security Backlog and Review Queue Expanded Faster Than Delivery

## Mainline & Merged Work

### The default branch changed decisively on 2026-05-22 and then went quiet again
- PR #117 (`build(deps): bump tower-http from 0.6.10 to 0.6.11 in the cargo group`) and PR #118 (`ci: add journal PR automation workflow`) both merged into `main` on 2026-05-22
- The same merge wave also landed PR #126 (`docs(journal): add 2026-05-19 through 2026-05-22 journal entries`) and PR #127 (`feat(rest): support request-scoped Basic auth`)
- That means the repo did ship one meaningful product-facing change after the 2026-05-22 baseline, but it shipped together with maintenance and journaling plumbing and then stopped taking further default-branch changes

## Issues
- Issue #34 closed when PR #127 merged, converting the long-running request-scoped auth ask into shipped behavior
- Five new follow-on issues opened immediately afterward on 2026-05-22: #128 (protected-route auth acceptance matrix), #129 (deny-by-default CORS and security headers), #130 (TLS and termination mode decision), #131 (session-aware rate limiting and backpressure), and #132 (audit logging and token-safe observability)
- So the repo traded one completed auth slice for a much clearer security-hardening backlog rather than reducing total planning surface

## Pull Request Queue
- The main live implementation branch remains PR #133 (`feat(rest): harden API security middleware`), with older release packaging work in PR #124 still waiting behind it
- The queue widened again on 2026-05-25 with three maintenance branches: PR #137 (actions updates), PR #138 (`serde_json`), and PR #139 (opentelemetry group updates)
- PR #140 (`docs: plan MCP as an API-layer surface`) also joined the queue the same day, while journal PRs #134 and #135 are still open on top of everything else
- The result is a repo whose next steps are well defined, but whose review queue is growing faster than `main` is absorbing it

## CI Health
- The 2026-05-22 merge wave validated cleanly: PRs #117, #118, #126, and #127 all produced successful `CI` and `Security` runs on `main`
- Recent scheduled `Security` on `main` also succeeded on 2026-05-25
- The only fresh blemish is maintenance automation noise: one `Dependabot Updates` run failed on 2026-05-25 while the other dependency-update runs passed, so core branch health looks fine even if the automated upkeep lane is not perfectly clean
