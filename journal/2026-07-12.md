# 2026-07-12 — `main` and `devel` Both Advanced While the Reserved Current-GVMD Route Contract Merged

## Mainline & `devel` Delivery
- After the published 2026-07-10 journal cutoff, `main` advanced twice on 2026-07-10 through PR #336 (`6fa1292`, track `rust-gvm` `main`) and PR #340 (`12c332b`, GitHub Actions dependency refresh).
- `devel` advanced the same day through PR #337 (track `rust-gvm` `devel`) and then PR #346, which merged the reserved authenticated REST/OpenAPI route families for issues #341-#344.
- That is a real posture change from the last entry: the current-GVMD expansion is no longer only an open review lane. The route contract is now merged on `devel`, while `main` has also caught up to the newer upstream tracking and CI dependency state.

## Validation
- The two new `main` commits both ran green across `CI`, `Security`, and `E2E`, so the default branch advanced without introducing a visible regression.
- The next scheduled `E2E` run on `main` also stayed green, which keeps the repo's immediate constraint on product sequencing rather than automation health.

## Attention
- The merged `#346` surface still returns explicit `501 not_implemented` placeholders for the newly reserved resources, so the next bottleneck is upstream typed-response coverage in `rust-gvm` and then real adapter implementations resource family by resource family.
