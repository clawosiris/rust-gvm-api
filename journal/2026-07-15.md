# 2026-07-15 — `main` Advanced Once While the Dependency Queue Collapsed into Two Fresh Red Lanes

## Mainline Shift
- Since the published 2026-07-14 journal checkpoint, `main` advanced once when PR #350 (`build(deps): bump gvm-mock-server from 2934cd8 to e39f3a4`) merged at squash commit `a5010c06aeaaf1889d03bc30012ca81560aacafb`.
- No new release or tag landed in the same window, so the default-branch movement is still a maintenance-step change rather than a packaging milestone.

## Queue Reshuffle
- Merging #350 caused Dependabot to close the earlier cargo tracking branches #349, #352, #353, #354, and #355 and replace them with a single new cargo-group PR, #357.
- That means the repo is no longer looking at the same seven-lane dependency wave described on 2026-07-14. The actionable queue has narrowed to the still-open GitHub Actions refresh #351 plus the replacement cargo rollup #357.

## Validation
- PR #357 did not inherit yesterday's exact red set. Its `CI` and `Security` suites passed, but `REST Discovery Scan E2E` failed immediately, leaving the replacement cargo lane blocked on an E2E regression rather than the earlier unit-test failures.
- PR #351 also stopped being a clean merge candidate after it rebased on the new `main`: it now fails both `Test (all features)` in `CI` and `REST Discovery Scan E2E`, even though the rest of its CI and security jobs still pass.

## Attention
- Near-term triage should now focus on whether the active dependency blockers are isolated to the replacement cargo rollup and the refreshed CodeQL action branch, or whether the post-merge environment exposed a broader E2E compatibility problem.
- The important shift is that yesterday's failing branches are gone, but the maintenance backlog did not get healthier overall: it condensed into two newer blocked PRs that still need diagnosis before the dependency queue can clear.
