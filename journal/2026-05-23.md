# 2026-05-23 — `main` Absorbed Basic Auth, Journal Automation, and a Fresh Security-Hardening Follow-On Queue

## Mainline & Merged Work

### The default branch moved heavily on 2026-05-22 and landed both delivery work and automation cleanup
- PR #117 (`build(deps): bump tower-http from 0.6.10 to 0.6.11 in the cargo group`) merged into `main`
- PR #126 (`docs(journal): add 2026-05-19 through 2026-05-22 journal entries`) merged and cleared the journal backlog that had built up since the prior baseline
- PR #118 (`ci: add journal PR automation workflow`) merged, so the repo now has the same journal automation lane as the rest of the ecosystem
- PR #127 (`feat(rest): support request-scoped Basic auth`) merged on the same day and closed issue #34

## Issues
- Five new REST hardening issues were opened on 2026-05-22: #128 (protected-route auth acceptance matrix), #129 (deny-by-default CORS and security headers), #130 (TLS/termination-mode decision), #131 (session-aware rate limiting/backpressure), and #132 (audit logging and token-safe observability)
- Issue #34 closed with the #127 Basic-auth merge
- So the repo both delivered one concrete REST auth slice and immediately expanded the next security-focused backlog behind it

## Pull Request Queue
- The next implementation lane is PR #133 (`feat(rest): harden API security middleware`), opened on 2026-05-22 and waiting on review rather than repairs
- PR #124 (`build(release): automate Debian and Arch package artifacts`) is still open and now sits behind a busier `main`, but it remains the key release/distribution branch in flight
- Older maintenance PRs #107 and #108 are still behind and remain the stale portion of the queue while the product-facing work is moving faster

## CI Health
- `main` stayed green through the 2026-05-22 merge burst: both push-triggered `CI` and `Security` runs succeeded after the merges
- PR #133 also started from a clean state with successful `CI` and `Security` runs on 2026-05-22
- This is the strongest current CI signal in the four-repo set: active changes are landing, and the follow-on branch is already green
