# 2026-07-10 — PR `#346` Turned the Current-GVMD Backlog into an Explicit Reserved-Route Delivery Lane

## Mainline & Releases

### `main` still did not move after the 2026-07-07 cutoff
- No new default-branch commit, merged non-journal PR, or release/tag shipped after the last published journal entry.
- The meaningful repo change is therefore still in the open queue rather than in a newly published artifact.

## Pull Request Queue
- PR `#346` opened on 2026-07-08 and reserves authenticated REST/OpenAPI routes for the issue `#341`-`#344` families: agent management, generic assets/configs, OCI-image and web-application targets, and report/operating-system subresources.
- Rather than implementing raw GMP XML handling downstream, the PR deliberately returns `501 not_implemented` placeholders until `rust-gvm` exposes typed response support for the needed list/get payloads.
- Copilot review comments were addressed on 2026-07-09 and the threads were resolved, leaving human approval and upstream typed-response availability as the remaining gating points.

## Project Direction
- That is a real posture change from the 2026-07-07 state. The repo is no longer only naming the missing API surface through issues `#341`-`#344`; it now has a concrete contract-first PR that fixes route names and auth boundaries before full adapter work lands.
- In practical terms, `rust-gvm-api` has moved from backlog definition into a reviewable delivery lane for current-GVMD parity while still preserving the rule that GMP XML processing stays in `rust-gvm`.

## Attention
- No new default-branch CI or security regression emerged alongside this shift, so the immediate constraint is coordination rather than automation health.
- Near-term attention should stay on upstream typed response coverage in `rust-gvm` and on replacing the reserved `501` handlers resource family by resource family once those models exist.
