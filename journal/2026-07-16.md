# 2026-07-16 — Docs-Aware Gates Landed and Branch-Specific E2E Protection Is Live

## Mainline Delivery
- Since the published 2026-07-15 journal entry, `main` advanced twice.
- PR [#359](https://github.com/clawosiris/rust-gvm-api/pull/359) merged the MCP roadmap documentation update after an explicit admin override of the already-known raw E2E failure.
- PR [#364](https://github.com/clawosiris/rust-gvm-api/pull/364) then merged the docs-aware CI and E2E gate work, adding the shared changed-file classifier, keeping the required `CI` context always present, and adding branch-specific `REST Discovery Scan E2E (main)` and `REST Discovery Scan E2E (devel)` gate contexts.

## Policy State
- Issues [#361](https://github.com/clawosiris/rust-gvm-api/issues/361), [#362](https://github.com/clawosiris/rust-gvm-api/issues/362), and [#363](https://github.com/clawosiris/rust-gvm-api/issues/363) are now closed by the gate migration.
- Branch protection has moved off the legacy raw `REST Discovery Scan E2E` required context: `main` now requires `Security`, `CI`, and `REST Discovery Scan E2E (main)`, while `devel` requires `Security`, `CI`, and `REST Discovery Scan E2E (devel)`.

## Active Attention
- The raw REST discovery E2E job is still red on `main` at commit `c929101`, including the scheduled run after the gate merge, so the underlying report-export / `gvmd` abort regression remains unresolved.
- New PR [#360](https://github.com/clawosiris/rust-gvm-api/pull/360) opened a first MCP implementation lane, but it is already dirty against the moved `main` and inherits the same raw E2E failure; it should be treated as a live implementation lane, not as evidence that the mainline E2E problem is fixed.
