# 2026-07-17 — The MCP Lane Moved onto Devel

## Delivery Policy
- PR [#360](https://github.com/clawosiris/rust-gvm-api/pull/360), the first MCP implementation lane, was retargeted from `main` to `devel` after the 2026-07-16 journal entry.
- The branch then merged current `devel` at commit `3676c90`, clearing the earlier dirty-branch state and making the PR mergeable against the development integration line.

## Validation
- The synchronized head is green across the aggregate `CI` and `Security` gates, including stable, MSRV, all-features, packaging, audit, deny, and SAST coverage.
- The branch-specific `REST Discovery Scan E2E (devel)` gate remains red because the underlying raw discovery scan still fails; PR #360 is therefore blocked despite being mergeable.

## Mainline State
- `main` did not advance after the previous journal entry, and the scheduled E2E run at `c929101` repeated the already-recorded discovery/report-export failure. This is not a new regression or recovery.
