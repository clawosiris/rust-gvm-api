# 2026-08-14 — Scheduled E2E Hit an External Registry Timeout

## Actionable CI Regression
- Scheduled [E2E run 31767901730](https://github.com/greenbone-hive/rust-gvm-api/actions/runs/31767901730) failed on unchanged `main` commit `19b4d1a6` after the preceding scheduled run passed.
- The compose-backed runtime could not start because Docker timed out contacting `registry.community.greenbone.net` while pulling the `report-formats` image. The discovery-scan suite was skipped and the required `REST Discovery Scan E2E (main)` gate failed closed.
- No default-branch commit, non-journal pull-request or issue change, release, or tag accompanied the regression, so the immediate attention point is registry availability or pull resilience rather than a repository code change.
