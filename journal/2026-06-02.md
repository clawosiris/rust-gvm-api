# 2026-06-02 — `main` Restored the Report-Export Contract, Closed the Original API Umbrella, and Left Runtime/Test Follow-Through Waiting Off-Branch

## Mainline & Merged Work

### The repo's only substantive June branch movement was specification work, but that work matters because it re-stabilized an external contract
- PR #194 (`spec(rest): restore report export contract`) merged on 2026-06-01 and put the report-export shape back into the OpenAPI and supporting REST spec files, so `main` regained a documented client contract rather than only carrying runtime ambition in issue form
- The earlier same-day merge of PR #191 only carried the 2026-05-31 journal entry forward, so the meaningful default-branch change in this window is the report-export contract restoration, not another documentation catch-up step
- That makes this entry narrower than the 2026-05-31 burst, but still important: the repo tightened the public REST surface again before the runtime branch trying to implement it was merged

## Issues
- Issue #193 (`feat(rest): add report export endpoint with report-format selection`) opened on 2026-06-01 and remains open, which cleanly separates the restored spec contract from the still-pending implementation work
- The long-running umbrella issue #1 (`feat: RESTful API server (gvm-rest-api)`) closed later that day, which is a strong governance signal that the repo now considers the top-level REST server objective decomposed enough to stop tracking as one monolith
- There was no comparable wave of newly opened sub-issues in this window, so backlog motion was concentrated in one new implementation issue and one major umbrella closure

## Pull Request Queue
- The queue now centers on runtime and verification follow-through rather than additional spec churn: PR #195 implements the report-export endpoint, PR #196 expands E2E coverage, and PR #190 still carries the weekly performance smoke lane
- Fresh dependency work also appeared overnight through PRs #197 and #198, so the open set widened again immediately after `main` absorbed the spec fix
- Relative to the previous journal entry, the repo is now in a cleaner but more explicit split state: the client-facing contract is back on `main`, while the code and test branches that must honor it are still waiting in review

## CI Health
- The push for PR #194 passed `CI` and `E2E` on 2026-06-01, so the spec restoration did not destabilize the stricter verification posture the repo established at the end of May
- Scheduled `Security` and scheduled `E2E` runs on 2026-06-01 also completed successfully, which keeps the baseline signal on `main` green across both security and integration lanes
- The only fresh noise is dependency automation rather than repo logic: a `github_actions` dynamic update failed on 2026-06-02 while the paired cargo, docker, and docker-compose update lanes succeeded
