# 2026-06-09 — `main` Landed a Six-PR REST Cleanup Burst and Turned the Remaining Contract Work into a Tighter Bug Queue

## Mainline & Merged Work

### The repo compressed a large amount of user-facing REST work into one 2026-06-08 landing burst
- Since the 2026-06-04 journal entry, `main` absorbed six merges in one day: PR #214 added host and NVT catalog reads, PR #219 pushed REST pagination into gvmd-backed filters, PR #223 fixed URL-form query decoding, PR #224 refreshed the actions stack, PR #225 pushed collection pagination into filters, and PR #235 tightened session runtime lifecycle handling
- Five of those six merges directly changed the runtime contract or request handling path rather than just polishing docs or tests, so this was another substantive surface-correction burst and not just maintenance churn
- The shape of the work is consistent with the repo's early-June pattern: broaden the REST surface, then immediately remove semantic mismatches around pagination, decoding, and session behavior before they fossilize into the public contract

## Issues
- Nine issues opened in this window: #226, #227, #228, #229, #230, #231, #232, #233, and #234
- One of them already closed through same-day merged work: #232 (session lifecycle/auth cleanup) closed when PR #235 landed
- Older backlog also burned down aggressively in the same burst: issues #171, #200, #210, and #216 all closed on 2026-06-08 as their corresponding fixes merged into `main`
- The result is a cleaner problem inventory than the repo had on 2026-06-04. The unresolved queue is now dominated by explicit contract-hardening and mapping bugs, not by vague pagination or auth ambiguity

## Pull Request Queue
- The open implementation queue is now short and targeted: PR #236 uses typed report detail commands, and PR #237 preserves unknown open-enum contract values
- That queue lines up directly with the newest issue set: report/result semantics, enum safety, and OpenAPI/runtime alignment are the next pressure points after the 2026-06-08 cleanup burst
- In other words, the repo has mostly succeeded at collapsing a wide REST correctness backlog into two active branches plus a bounded list of follow-on bugs

## CI Health
- `main` is presently strong. The latest 2026-06-08 push runs cleared `CI`, `E2E`, and `Security`, and there is no fresh evidence of branch-wide instability
- The open PR signal is mixed but interpretable rather than chaotic: PR #236 passed `CI` and `Security` but failed `E2E`, while PR #237 passed `E2E` and `Security` but failed `CI`
- That means the repo's current risk is no longer "everything is noisy." It is narrower: each of the two open branches is tripping one different verification lane, so review and fix throughput on those branches matters more than mainline health right now
