# 2026-05-15 — Mainline Absorbed Two Real App Changes and a Module Split, Leaving One Easy Green Maintenance Branch and One Broken Telemetry Bump

## Mainline & Merged Work

### `main` moved heavily after the 2026-05-13 journal baseline, and this time the change was not just maintenance churn
- `main` picked up six commits on 2026-05-13: PR #102 (`feat(session): enforce expiry and disconnect cleanup semantics (#98)`), PR #105 (`docs(journal): add 2026-05-10 journal entry`), PR #109 (`build(deps): bump the cargo group with 2 updates`), PR #112 (`docs(journal): add 2026-05-11 through 2026-05-13 journal entries`), PR #114 (`feat(app): add audit logging and execution tracing (#99)`), and PR #115 (`refactor: split oversized gateway lib modules (#113)`)
- That is a meaningful default-branch wave: session lifecycle semantics tightened, audit/logging and tracing landed, the oversized gateway libraries were split, and the cargo maintenance lane also merged cleanly
- Compared with the last baseline, the repo is no longer waiting on a hypothetical feature lane; the work actually landed on `main`

## Issues
- Issue #113 (`refactor: split oversized lib.rs files in gvm-gateway-domain and gvm-gateway-app`) was opened and closed in the same 2026-05-13 window
- Issues #98 and #99 were also closed as the merged session-lifecycle and audit/tracing work landed
- So the repo did not just merge code; it also burned down the linked design backlog that had been attached to those implementation lanes

## Pull Request Queue
- PR #107 (`ci(deps): bump the actions group with 2 updates`) is still the easy maintenance merge: the visible check surface is green, but the branch is now `BEHIND` `main` after the 2026-05-13 merge wave
- PR #108 (`build(deps): bump the opentelemetry group across 1 directory with 3 updates`) is still the broken lane, failing `Clippy`, multiple test jobs, coverage, and MSRV-adjacent validation while security-only checks remain green
- The bigger queue cleanup already happened when PRs #106, #110, and #111 were closed and their useful work was absorbed by the merged branches, so the open decision surface is now small and easy to read

## CI Health
- `main` produced repeated successful `CI`, `Security`, and `Dependabot Updates` runs on 2026-05-13 as the merge wave landed, which gives the default branch a much fresher and cleaner signal than it had at the last journal baseline
- The strongest branch-level contrast is now simple: #107 is healthy and low risk, while #108 is still obviously broken
- This repo looks operationally healthy again; the next question is queue hygiene, not branch-wide instability
