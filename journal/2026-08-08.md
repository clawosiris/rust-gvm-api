# 2026-08-08 — Endpoint PRs Address Their Recorded Acceptance Blockers

## Compliance Workflow
- PR [#418](https://github.com/greenbone-hive/rust-gvm-api/pull/418) added a `usageType` discriminator across the scan-config domain, REST response, and OpenAPI schema, allowing clients to distinguish scan configurations from policies even though the pinned backend still returns both families from the collection.
- Its compose-gated E2E now requires a policy, creates and starts an audit, waits for a terminal state, retrieves the linked compliance report, and verifies that the audit never leaks into the scan-task route. This addresses the two acceptance blockers recorded on 2026-08-07.

## Host Write Contract
- PR [#426](https://github.com/greenbone-hive/rust-gvm-api/pull/426) now rejects unknown host-modification fields, including the unsupported `value`, and rejects any `ultimate` delete query with `400`.
- The lower-layer `value` and `ultimate` parameters were removed, preventing non-REST callers from reintroducing the silently ignored semantics. Regression coverage was added at the REST boundary.

## Validation State
- Both updated heads pass their reported CI, security, and `devel` E2E matrices. Both PRs remain open with stale changes-requested decisions and require follow-up review before merge.
