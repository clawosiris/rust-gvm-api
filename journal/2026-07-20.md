# 2026-07-20 — The Actions Refresh Has Two Confirmed Blockers

## Review Movement
- PR [#351](https://github.com/clawosiris/rust-gvm-api/pull/351), the open CodeQL Actions pin refresh, received a substantive follow-up review after the 2026-07-18 journal entry.
- The workflow diff remains limited to the expected `github/codeql-action/upload-sarif` update, but the review changed the PR's state from previously approved to explicitly not merge-ready.

## Blocking Validation
- `Test (all features)` fails `lib_test::service_report_export_jobs_expire_after_terminal_retention` because the report export job becomes `NotFound` before its expected retention window.
- `REST Discovery Scan E2E` also fails with HTTP 502 `backend_unavailable` while fetching the report, matching the discovery/report-export failure family already blocking the MCP lane and scheduled `main` E2E runs.
- PR #351 remains open and behind `main`; the dependency-only change should not be merged until both blocking checks recover or their underlying regressions are resolved.
